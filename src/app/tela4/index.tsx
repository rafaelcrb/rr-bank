import * as React from 'react';
import { View, Text } from 'react-native';

export interface Tela4PageProps {
}

export default function Tela4Page (props: Tela4PageProps) {
    return (
      <View style={{backgroundColor: 'red', flex: 1}}>
         <Text>Tela4Page</Text>
      </View>
    );
}
